# DesignMakr

Text-to-image SaaS for preschools built with Next.js App Router.

## Quick start
1. `npm install`
2. Copy `.env.example` to `.env.local` and fill values
3. `npm run dev`

## ENV
- `OPENAI_API_KEY`
- `NEXTAUTH_SECRET`
- `NEXTAUTH_URL`
